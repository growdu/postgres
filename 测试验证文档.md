# 测试验证文档

## 1. 目标与范围

本文件用于记录设计1与设计2的分项验证方法与通过标准。

- 设计1：目录模型、用户接口与消息表能力
- 设计2：系统表逻辑复制通路（数据面）

说明：设计3/设计4的端到端联调请参考《集成验证文档》。

## 2. 基础环境

- 代码目录：`/Users/growduduan/cwork/postgresql-3`
- 命令约束：所有命令统一使用 `rtk` 前缀
- 推荐安装路径：`tmp_install/usr/local/pgsql`

示例：

```bash
rtk make -s -j8
rtk make -s install prefix=/Users/growduduan/cwork/postgresql-3/tmp_install/usr/local/pgsql
```

## 3. 设计1验证

详细步骤已写入《设计一——逻辑复制新增系统表与用户接口设计》“测试验证步骤”章节（第15节）。

### 3.1 验证重点

- `CREATE/ALTER PUBLICATION ... WITH (ddl=...)` 生效
- `CREATE/ALTER SUBSCRIPTION ... WITH (ddl=...)` 生效
- `pg_publication_sync` 结构完整
- `Q/A/D` 消息写入行为符合预期
- 清理函数行为正确

### 3.2 通过标准

- `pubddl/subddl` 字段值与输入 token 对齐
- `pg_publication_sync` 中 `msgtype` 与 payload 对齐
- 清理后无残留异常状态记录

## 4. 设计2验证

详细步骤已写入《设计二——逻辑复制支持同步系统表概要设计》“测试验证步骤”章节（第14节）。

### 4.1 验证重点

- publication 对 `pg_publication_sync` 的隐式成员语义正确
- 禁止显式 `ADD TABLE pg_publication_sync`
- `pgoutput` 基于 `pfsyncpubid` 的行级过滤正确
- subscriber 侧系统表落地正确
- 本期仅覆盖 `INSERT` 路径

### 4.2 通过标准

- 不同 publication 不串流
- 订阅端仅接收匹配 `pfsyncpubid` 的消息
- 订阅端 `pg_publication_sync` 行数与发布端预期一致

## 5. 建议执行顺序

1. 先跑设计1分项验证，确认 DDL 接口和消息入表正确。
2. 再跑设计2分项验证，确认系统表复制通路与过滤正确。
3. 分项全部通过后再进入设计3/4集成验证。
