# 设计1：新增系统表 `pg_publication_sync`

更新时间：2026-04-17

对应测试文档：  
[新增系统表-测试验证步骤.md](/Users/growduduan/cwork/postgresql-2/新增系统表-测试验证步骤.md)

## 1. 目标

新增系统目录表 `pg_publication_sync`，作为逻辑复制 DDL 自动同步的消息表。

关键约束：

1. 无行级 `oid` 字段。
2. append-only，只写入不删除。
3. 不做“配置状态行”，只保留消息行。

## 2. 表结构

定义文件：`src/include/catalog/pg_publication_sync.h`。  
关系 OID：`9352`。

字段分组：

1. 路由与元信息
   * `pfsyncpubid`：消息所属 publication（发布端 OID）
   * `pfsyncobjid`：消息事件 ID
   * `pfsyncddl`：DDL 类型 bitmask
   * `pfsyncenabled`：保留字段（当前写入固定 `true`）
   * `pfsynclsn`：消息写入时记录的 LSN
   * `pfsyncts`：消息写入时间（timestamptz）
2. 消息载荷
   * `pfsyncmsgtype`：主分类字段（当前仅 `Q/A/D`）
   * `pfsynctargettable`
   * `pfsyncddlsql`
   * `pfsyncsearchpath`
   * `pfsyncpublicationlist`（保留，当前不参与订阅端决策）
   * `pfsyncextra`

## 3. 消息模型

`pg_publication_sync` 每行就是一条消息：

1. `Q`：DDL SQL 重放消息
2. `A`：将对象加入 publication 的通知消息
3. `D`：将对象移出 publication 的通知消息

不再使用 `p/r/n/o` 行类型模型。

## 4. 索引

1. 唯一索引：`pg_publication_sync_map_index`  
   键：`(pfsyncpubid, pfsyncnspid, pfsyncrelid, pfsyncobjid, pfsyncsubid)`
2. 普通索引：`pg_publication_sync_pubid_index`（`pfsyncpubid`）

## 5. 写入入口

1. `ProcessUtility` DDL 捕获写入 `Q`。
2. publication 成员变更写入 `A/D`。

所有写入均为 INSERT，不更新、不删除历史消息。

## 6. 生命周期

1. 消息持久保留，用于复制、重试与审计。
2. publication 删除时不反向清理历史消息（只新增必要 `D` 消息）。
3. 提供清理函数：`select pg_publication_sync_prune();`
   * 按逻辑槽最小 restart LSN 与时间窗口联合裁剪历史消息。
