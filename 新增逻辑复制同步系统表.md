# 设计1：新增系统表 `pg_publication_sync`

更新时间：2026-04-17

对应测试文档：  
[新增系统表-测试验证步骤.md](/Users/growduduan/cwork/postgresql-2/新增系统表-测试验证步骤.md)

## 1. 目标

新增一个系统目录表 `pg_publication_sync`，用于承载“publication 级 DDL 同步配置”和“对象级同步消息”。

本设计只定义目录模型，不讨论复制通道与 apply 执行细节。

## 2. 系统表定义

定义文件：`src/include/catalog/pg_publication_sync.h`。

表 OID：`9352`，关系名：`pg_publication_sync`。

核心字段：

1. 固定字段
   * `oid`
   * `pfsyncpubid`：关联 `pg_publication.oid`
   * `pfsynckind`：`p/r/n/o`
   * `pfsyncnspid`、`pfsyncrelid`、`pfsyncobjid`、`pfsyncsubid`
   * `pfsyncenabled`
   * `pfsyncddl`：DDL 类型 bitmask
2. 变长字段
   * `message_type`
   * `target_table`
   * `ddl_str`
   * `publication_list`
   * `search_path`
   * `pfsyncextra`

## 3. 行类型语义

`pfsynckind` 当前重点使用两类：

1. `p`（publication 默认配置行）
   * 由 `CREATE/ALTER PUBLICATION ... WITH (ddl=...)` 维护。
   * 用于表达 publication 的 DDL 能力基线。
2. `o`（对象级消息行）
   * 用于承载具体 DDL 事件消息（`Q/A/D`）。
   * 行内容面向订阅端 apply 分发。

`r/n` 保留未来扩展。

## 4. 索引与约束

已定义索引：

1. 主键：`pg_publication_sync_oid_index`（`oid`）。
2. 唯一索引：`pg_publication_sync_map_index`  
   键：`(pfsyncpubid, pfsynckind, pfsyncnspid, pfsyncrelid, pfsyncobjid, pfsyncsubid)`。
3. 普通索引：`pg_publication_sync_pubid_index`（`pfsyncpubid`）。

并创建 syscache：`PUBLICATIONSYNC`。

## 5. 写入入口

### 5.1 publication 默认行（`p`）

实现入口：`src/backend/commands/publicationcmds.c`。

`upsert_publication_sync_default(pubid, ddlmask)` 负责：

1. 若已有 `p` 行则更新 `pfsyncddl/pfsyncenabled`。
2. 若不存在则插入新行。
3. 建立到 publication 的自动依赖。

### 5.2 对象消息行（`o`）

当前消息来源有两类：

1. DDL 捕获消息（`message_type='Q'`，详见设计3）。
2. publication 成员变更消息（`message_type='A'/'D'`）：
   * `ALTER PUBLICATION ... ADD TABLE` 产生 `A`；
   * `ALTER PUBLICATION ... DROP TABLE` 产生 `D`；
   * `DROP PUBLICATION` 时为关联成员发出 `D`。

## 6. 依赖管理与生命周期

1. `p` 行依赖 `pg_publication`，publication 删除后会清理。
2. `o` 行同样记录 publication 依赖。
3. 行删除与对象删除遵循 catalog 依赖框架，不依赖应用层手工清理。

## 7. 与后续设计的接口

本设计对外暴露三类关键字段供后续使用：

1. `pfsyncddl`：发布端/订阅端 DDL 能力过滤。
2. `message_type`：`Q/A/D` 分发协议。
3. `target_table`、`ddl_str`、`publication_list`、`search_path`：apply 执行上下文。
