# 逻辑复制支持系统表同步功能测试验证步骤

## 1. 目标与边界

本用例只验证一个能力：

* 逻辑复制可以同步“白名单系统表”。

本用例**不验证**以下能力：

* `pubddl/subddl` 语义协商；
* `pg_publication_sync` 控制面建模正确性；
* DDL 消息同步链路。

`pg_publication_sync` 在本文件中仅作为“系统表样例”。

## 2. 测试前提

* 发布端/订阅端均为当前分支构建版本；
* 两端都已启用 `wal_level=logical`；
* 已创建复制用户，网络可互通；
* 订阅端数据库中存在系统表 `pg_catalog.pg_publication_sync`（同版本节点默认存在）。

## 3. 发布端准备

```sql
-- publication 仍然以用户表为主体；pg_publication_sync 会被隐式纳入
CREATE TABLE pub_sysrel_t1(id int primary key, v text);
CREATE PUBLICATION pub_sysrel FOR TABLE pub_sysrel_t1 WITH (ddl='table,index');

-- 验证系统表被自动纳入发布集合
SELECT schemaname, tablename
  FROM pg_publication_tables
 WHERE pubname = 'pub_sysrel'
 ORDER BY 1, 2;
```

预期：

* publication 创建成功；
* publication 包含用户表 `public.pub_sysrel_t1`；
* 未执行 `ADD TABLE pg_catalog.pg_publication_sync`；
* 查询结果包含 `pg_catalog.pg_publication_sync`。

## 4. 订阅端准备

```sql
-- 该能力当前建议 copy_data=false，只验证增量同步
CREATE SUBSCRIPTION sub_sysrel
CONNECTION 'host=... port=... dbname=... user=... password=...'
PUBLICATION pub_sysrel
WITH (copy_data = false);
```

预期：

* 订阅创建成功；
* apply worker 正常启动，无 “cannot use relation ... as logical replication target” 报错。

## 5. 增量同步验证（核心）

### 5.1 发布端制造变更

```sql
-- 在已开启 ddl 的用户表 publication 上触发 DDL
ALTER TABLE pub_sysrel_t1 ADD COLUMN c2 int;
CREATE INDEX idx_pub_sysrel_t1_v ON pub_sysrel_t1(v);
```

### 5.2 订阅端观察结果

```sql
SELECT p.pubname,
       s.pfsynckind,
       s.pfsyncenabled,
       s.pfsyncddl,
       s.message_type,
       s.search_path,
       s.target_table,
       s.ddl_str,
       s.publication_list
  FROM pg_publication_sync s
  LEFT JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE (s.pfsynckind = 'p' AND p.pubname = 'pub_sysrel')
    OR (s.pfsynckind = 'o'
        AND position('pub_sysrel' in coalesce(s.publication_list, '')) > 0
        AND (position('ALTER TABLE pub_sysrel_t1' in coalesce(s.ddl_str, '')) > 0
             OR position('CREATE INDEX idx_pub_sysrel_t1_v' in coalesce(s.ddl_str, '')) > 0))
 ORDER BY s.pfsynckind, s.oid;
```

预期：

* 能查到 `pub_sysrel` 的 `pfsynckind='p'` 配置记录；
* 能查到由 `pub_sysrel_t1` DDL 触发的 `pfsynckind='o'` 捕获记录；
* `pfsynckind='o'` 的行中：
  * `message_type` 固定为 `Q`（表示 Query SQL 重放消息）；
  * `search_path` 记录发布端执行该 DDL 时的 search_path；
  * `target_table` 为目标对象名（可为空，取决于语句类型）；
  * `ddl_str` 为原始 DDL SQL 文本；
  * `publication_list` 包含该 DDL 相关 publication 名称。

### 5.3 订阅先建立后触发变更（关键回归）

目的：验证“只要订阅端已连接，`pg_publication_sync` 后续新增记录都能同步”。

发布端：

```sql
CREATE TABLE pub_sysrel_late_t1(id int primary key, v text);
CREATE PUBLICATION pub_sysrel_late FOR TABLE pub_sysrel_late_t1 WITH (ddl='table,index');
```

订阅端：

```sql
CREATE SUBSCRIPTION sub_sysrel_late
CONNECTION 'host=... port=... dbname=... user=... password=...'
PUBLICATION pub_sysrel_late
WITH (copy_data = false);
```

发布端（订阅建立后再触发用户表 DDL）：

```sql
ALTER TABLE pub_sysrel_late_t1 ADD COLUMN late_c int;
```

订阅端验证：

```sql
SELECT p.pubname, s.pfsynckind, s.message_type, s.ddl_str, s.publication_list
  FROM pg_publication_sync s
  LEFT JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE (s.pfsynckind = 'p' AND p.pubname = 'pub_sysrel_late')
    OR (s.pfsynckind = 'o'
        AND position('pub_sysrel_late' in coalesce(s.publication_list, '')) > 0
        AND position('pub_sysrel_late_t1' in coalesce(s.ddl_str, '')) > 0)
 ORDER BY s.pfsynckind, s.oid;
```

预期：

* 能查到 `pub_sysrel_late` 对应记录；
* 既有 `pfsynckind='p'`，也有订阅建立后产生的 `pfsynckind='o'`；
* 证明订阅建立后产生的 `pg_publication_sync` 变更会实时同步。

### 5.4 `message_type` 框架分支验证（A/D 预留）

说明：

* 当前订阅端 apply 分发器已包含 `Q/A/D` 三个分支；
* 其中只有 `Q` 已实现，`A` 与 `D` 为预留分支，当前会返回 `FEATURE_NOT_SUPPORTED`；
* 该行为用于保证后续扩展时不需要重构主分发路径。

预期：

* 当接收到 `message_type='A'` 或 `message_type='D'` 时，apply worker 报错并提示“not supported yet”；
* 当接收到未知类型（如 `X`）时，报错并提示“Supported message types are Q/A/D; only Q is implemented now.”

### 5.5 捕获/应用过滤规则验证（with ddl + publication 范围 + publication_list）

目标：

* 捕获端：仅当 publication 的 `with (ddl=...)` 包含该 DDL 类型，且对象范围命中（`FOR TABLE` / `FOR TABLES IN SCHEMA` / `FOR ALL TABLES`）时才写 `pg_publication_sync`；
* 应用端：仅当订阅 `subddl` 命中且 `publication_list` 与订阅 publication 列表有交集时才执行 DDL。

发布端：

```sql
CREATE SCHEMA app;
CREATE TABLE t_scope_tbl(id int primary key);
CREATE TABLE app.t_scope_schema_tbl(id int primary key);
CREATE TABLE t_scope_other(id int primary key);

CREATE PUBLICATION pub_scope_tbl FOR TABLE t_scope_tbl WITH (ddl='table');
CREATE PUBLICATION pub_scope_schema FOR TABLES IN SCHEMA app WITH (ddl='table');
CREATE PUBLICATION pub_scope_all FOR ALL TABLES WITH (ddl='table');
CREATE PUBLICATION pub_scope_idx FOR TABLE t_scope_tbl WITH (ddl='index');

ALTER TABLE t_scope_tbl ADD COLUMN c_tbl int;
ALTER TABLE app.t_scope_schema_tbl ADD COLUMN c_schema int;
ALTER TABLE t_scope_other ADD COLUMN c_all int;
```

订阅端（只订阅 `pub_scope_tbl`）：

```sql
CREATE SUBSCRIPTION sub_scope
CONNECTION 'host=... port=... dbname=... user=... password=...'
PUBLICATION pub_scope_tbl
WITH (copy_data = false, ddl='table');
```

验证 SQL（订阅端）：

```sql
-- 1) publication_list 交集命中：应执行
SELECT EXISTS (
  SELECT 1 FROM pg_attribute
   WHERE attrelid = 't_scope_tbl'::regclass
     AND attname = 'c_tbl' AND NOT attisdropped
) AS tbl_applied;

-- 2) publication_list 无交集：不应执行
SELECT EXISTS (
  SELECT 1 FROM pg_attribute
   WHERE attrelid = 'app.t_scope_schema_tbl'::regclass
     AND attname = 'c_schema' AND NOT attisdropped
) AS schema_applied;

-- 3) with ddl 类型不命中（index publication 不应捕获 table ddl）
SELECT coalesce(bool_or(position('pub_scope_idx' in publication_list) > 0), false) AS has_idx_pub
  FROM pg_publication_sync
 WHERE pfsynckind = 'o'
   AND position('ALTER TABLE t_scope_tbl ADD COLUMN c_tbl' in ddl_str) > 0;
```

预期：

* `tbl_applied = true`；
* `schema_applied = false`（即使 `pg_publication_sync` 行同步到了订阅端，DDL 也不会执行）；
* `has_idx_pub = false`，说明捕获端已按 `with (ddl=...)` 类型过滤。

### 5.6 `FOR TABLES IN SCHEMA / FOR ALL TABLES` 自动纳管与剔除验证

目标：

* 当 publication 是 `FOR TABLES IN SCHEMA` 或 `FOR ALL TABLES` 时，订阅在线期间发布端 `CREATE TABLE` 后，订阅端自动将新表纳入复制（`pg_subscription_rel` 自动建 READY 记录）；
* 新表后续 DML 自动 apply；
* 发布端 `DROP TABLE` 后，订阅端对应关系自动清理（包括 `pg_subscription_rel` 映射）。

发布端：

```sql
CREATE SCHEMA sys_auto;
CREATE PUBLICATION pub_sys_auto_schema FOR TABLES IN SCHEMA sys_auto WITH (ddl='table');
CREATE PUBLICATION pub_sys_auto_all FOR ALL TABLES WITH (ddl='table');
```

订阅端：

```sql
CREATE SUBSCRIPTION sub_sys_auto_schema
CONNECTION 'host=... port=... dbname=... user=... password=...'
PUBLICATION pub_sys_auto_schema
WITH (copy_data = false, ddl='table');

CREATE SUBSCRIPTION sub_sys_auto_all
CONNECTION 'host=... port=... dbname=... user=... password=...'
PUBLICATION pub_sys_auto_all
WITH (copy_data = false, ddl='table');
```

发布端（schema publication）：

```sql
CREATE TABLE sys_auto.t_schema_auto(id int primary key, v text);
INSERT INTO sys_auto.t_schema_auto VALUES (1, 'schema-auto');
```

发布端（all tables publication）：

```sql
CREATE TABLE t_all_auto(id int primary key, v text);
INSERT INTO t_all_auto VALUES (1, 'all-auto');
```

订阅端验证：

```sql
SELECT count(*) AS schema_rows FROM sys_auto.t_schema_auto;
SELECT count(*) AS all_rows FROM t_all_auto;

SELECT count(*)
  FROM pg_subscription_rel sr
  JOIN pg_subscription s ON s.oid = sr.srsubid
  JOIN pg_class c ON c.oid = sr.srrelid
 WHERE (s.subname = 'sub_sys_auto_schema' AND c.relname = 't_schema_auto')
    OR (s.subname = 'sub_sys_auto_all' AND c.relname = 't_all_auto');
```

发布端删除：

```sql
DROP TABLE sys_auto.t_schema_auto;
DROP TABLE t_all_auto;
```

订阅端验证：

```sql
SELECT to_regclass('sys_auto.t_schema_auto') IS NULL AS schema_dropped;
SELECT to_regclass('t_all_auto') IS NULL AS all_dropped;

SELECT count(*)
  FROM pg_subscription_rel sr
  JOIN pg_subscription s ON s.oid = sr.srsubid
  JOIN pg_class c ON c.oid = sr.srrelid
 WHERE (s.subname = 'sub_sys_auto_schema' AND c.relname = 't_schema_auto')
    OR (s.subname = 'sub_sys_auto_all' AND c.relname = 't_all_auto');
```

预期：

* `schema_rows = 1`，`all_rows = 1`；
* 两个新表都自动出现在对应订阅的 `pg_subscription_rel`（计数为 `2`）；
* `DROP TABLE` 后两表在订阅端不存在，`pg_subscription_rel` 对应映射计数为 `0`。

## 6. 负向验证

```sql
-- 非白名单系统表应仍被拒绝（示例）
CREATE PUBLICATION pub_sysrel_bad FOR TABLE pg_catalog.pg_class;
```

预期：

* 报错拒绝；
* 错误为“系统表不支持该操作”同类语义。

## 7. 回归验证

```sql
-- 普通表发布能力不受影响
CREATE TABLE lr_user_tbl(id int primary key, v text);
CREATE PUBLICATION pub_user FOR TABLE lr_user_tbl;
DROP PUBLICATION pub_user;
DROP TABLE lr_user_tbl;
```

预期：

* 普通逻辑复制流程行为与改动前一致。

## 8. 清理

发布端：

```sql
DROP PUBLICATION IF EXISTS pub_sysrel;
DROP PUBLICATION IF EXISTS pub_sysrel_late;
DROP PUBLICATION IF EXISTS pub_sys_auto_schema;
DROP PUBLICATION IF EXISTS pub_sys_auto_all;
DROP TABLE IF EXISTS pub_sysrel_t1;
DROP TABLE IF EXISTS pub_sysrel_late_t1;
DROP SCHEMA IF EXISTS sys_auto CASCADE;
```

订阅端：

```sql
DROP SUBSCRIPTION IF EXISTS sub_sysrel;
DROP SUBSCRIPTION IF EXISTS sub_sysrel_late;
DROP SUBSCRIPTION IF EXISTS sub_sys_auto_schema;
DROP SUBSCRIPTION IF EXISTS sub_sys_auto_all;
```
