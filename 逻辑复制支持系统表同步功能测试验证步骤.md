# 逻辑复制支持系统表同步功能测试验证步骤

## 1. 目标与边界

本用例只验证一个能力：

* 逻辑复制可以同步“白名单系统表”。

本用例**不验证**以下能力：

* `pubddl/subddl` 语义协商；
* `pg_publication_sync` 控制面建模正确性；
* DDL 消息同步链路。

`pg_publication_sync` 在本文件中仅作为“系统表样例”。

## 2. 测试前提

* 发布端/订阅端均为当前分支构建版本；
* 两端都已启用 `wal_level=logical`；
* 已创建复制用户，网络可互通；
* 订阅端数据库中存在系统表 `pg_catalog.pg_publication_sync`（同版本节点默认存在）。

## 3. 发布端准备

```sql
-- 建一个只用于本用例的 publication
CREATE PUBLICATION pub_sysrel FOR TABLE pg_catalog.pg_publication_sync;

-- 验证系统表可发布
SELECT schemaname, tablename
  FROM pg_publication_tables
 WHERE pubname = 'pub_sysrel'
 ORDER BY 1, 2;
```

预期：

* publication 创建成功；
* 查询结果包含 `pg_catalog.pg_publication_sync`。

## 4. 订阅端准备

```sql
-- 该能力当前建议 copy_data=false，只验证增量同步
CREATE SUBSCRIPTION sub_sysrel
CONNECTION 'host=... port=... dbname=... user=... password=...'
PUBLICATION pub_sysrel
WITH (copy_data = false);
```

预期：

* 订阅创建成功；
* apply worker 正常启动，无 “cannot use relation ... as logical replication target” 报错。

## 5. 增量同步验证（核心）

### 5.1 发布端制造变更

```sql
-- 通过 DDL 操作触发 pg_publication_sync 新增/更新
CREATE PUBLICATION pub_sync_case WITH (ddl='table,index');
ALTER PUBLICATION pub_sync_case SET (ddl='view,function');
```

### 5.2 订阅端观察结果

```sql
SELECT p.pubname,
       s.pfsynckind,
       s.pfsyncenabled,
       s.pfsyncddl
  FROM pg_publication_sync s
  JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE p.pubname = 'pub_sync_case';
```

预期：

* 能查到 `pub_sync_case` 对应记录；
* `pfsynckind='p'`；
* `pfsyncenabled=true`；
* `pfsyncddl` 与发布端最新设置一致（本例应对应 `view,function` 的位掩码值）。

## 6. 负向验证

```sql
-- 非白名单系统表应仍被拒绝（示例）
CREATE PUBLICATION pub_sysrel_bad FOR TABLE pg_catalog.pg_class;
```

预期：

* 报错拒绝；
* 错误为“系统表不支持该操作”同类语义。

## 7. 回归验证

```sql
-- 普通表发布能力不受影响
CREATE TABLE lr_user_tbl(id int primary key, v text);
CREATE PUBLICATION pub_user FOR TABLE lr_user_tbl;
DROP PUBLICATION pub_user;
DROP TABLE lr_user_tbl;
```

预期：

* 普通逻辑复制流程行为与改动前一致。

## 8. 清理

发布端：

```sql
DROP PUBLICATION IF EXISTS pub_sync_case;
DROP PUBLICATION IF EXISTS pub_sysrel;
```

订阅端：

```sql
DROP SUBSCRIPTION IF EXISTS sub_sysrel;
```
