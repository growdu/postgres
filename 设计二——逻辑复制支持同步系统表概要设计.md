

# 逻辑复制支持同步系统表概要设计

## 1. 设计目标

让受控白名单系统表具备完整的逻辑复制通路能力，使其变更可以沿现有逻辑复制链路从发布端传输到订阅端。

具体目标包括：

1. 发布端对白名单系统表的变更能够产生日志并进入逻辑解码链路；
2. publication 层可将该系统表视为合法的可发布关系；
3. `pgoutput` 能正确输出该系统表的变更；
4. subscriber 能将该系统表视为合法的复制目标关系；
5. 订阅端最终能够把远端系统表的变更写入本地对应系统表。

---

## 2. 设计范围

本设计仅覆盖以下范围：

### 2.1 包含内容

* 白名单系统表判定机制；
* WAL / 逻辑日志抽取前提放开；
* publication 层 publishable 判定放开；
* `pgoutput` 对系统表的支持；
* subscriber 目标关系映射与打开检查；
* 对 `pg_publication_sync` 的隐式成员语义；
* 针对 `pg_publication_sync` 的行级 publication 路由过滤。

### 2.2 不包含内容

* `pg_publication_sync` 表结构设计；
* `pubddl/subddl` 用户接口语义；
* `Q/A/D` 业务解释；
* 订阅端自动执行 DDL；
* 订阅端如何变更 `pg_subscription_rel` 或其他控制元数据。

---

## 3. 设计原则

## 3.1 白名单原则

系统表逻辑复制能力必须严格限制在白名单范围内。
不允许泛化为“任意 catalog 都可逻辑复制”。

当前白名单仅包含：

* `PublicationSyncRelationId`（即 `pg_publication_sync`）

这样做的原因是：

1. PostgreSQL 原有逻辑复制默认排除系统表，这是重要安全边界；
2. 全量放开 catalog 会破坏现有假设，风险极高；
3. 设计2的目标只是为 DDL 消息表打通最小闭环，不是重构 catalog 复制语义。

---

## 3.2 database-local catalog 原则

本设计只支持 **database-local catalog**，不扩展到 **shared catalog**。

原因：

1. 逻辑复制按数据库维度工作；
2. apply worker 也是数据库内 worker；
3. shared catalog 涉及 cluster 级一致性，不适合直接套用当前 publication/subscription 模型；
4. 共享目录的复制语义远超本设计范围。

因此本设计明确限定：

> 仅允许 database-local catalog relation 进入系统表逻辑复制通路。

---

## 3.3 隐式成员原则

`pg_publication_sync` 不作为普通表由用户显式加入 publication。
其成员关系完全由 publication 的 DDL 能力配置隐式决定。

即：

* 当 publication 配置了 `WITH (ddl=...)`
* 且 `pubddl > 0`
* 则 publication 自动隐式包含 `pg_publication_sync`

同时必须禁止用户执行：

```sql
ALTER PUBLICATION ... ADD TABLE pg_publication_sync;
```

这样做可以避免：

* 显式成员与隐式成员语义冲突；
* `pg_publication_rel` 和隐式推导双重管理；
* 用户误把系统表当普通业务表操作。

---

## 3.4 append-only 动作范围原则

当前仅要求支持白名单系统表的：

* `INSERT`

不要求支持：

* `UPDATE`
* `DELETE`
* `TRUNCATE`

这是因为当前白名单表 `pg_publication_sync` 本身被定义为 append-only 消息表。
设计2应与设计1保持一致，只为本期所需的 `INSERT` 通路打通，不扩大动作范围。

---

## 3.5 行级路由原则

对于普通逻辑复制表，publication 过滤通常只在 relation 级进行。
但对于 `pg_publication_sync`，仅做 relation membership 判断是不够的。

原因是：

* `pg_publication_sync` 被多个开启 DDL 的 publication 隐式共享；
* 但表中的每一行消息只归属其中一个 publication；
* 该归属通过 `pfsyncpubid` 表达。

因此必须增加特殊规则：

> 对 `pg_publication_sync`，除 relation membership 外，还必须按 `pfsyncpubid` 做 tuple 级 publication 过滤。

---

## 4. 总体架构

设计2打通的逻辑复制链路如下：

```text
白名单系统表写入
    ↓
生成逻辑复制所需 WAL 信息
    ↓
logical decoding 不再因系统表身份忽略
    ↓
publication 视为可发布
    ↓
pgoutput 输出变更
    ↓
subscriber 接收并映射到本地同名系统表
```

对于 `pg_publication_sync`，在发送侧还多一层：

```text
relation membership
    +
tuple.pfsyncpubid publication 匹配
```

这样最终保证：

* publication A 只发送属于 A 的消息；
* publication B 只发送属于 B 的消息。

---

## 5. 白名单控制设计

## 5.1 新增统一接口

新增统一白名单判断接口，例如：

文件：

```c
src/backend/replication/logical/logicalsysrel.c
```

接口：

```c
bool IsLogicalRepSystemRelationOid(Oid relid);
```

## 5.2 接口职责

该接口用于统一判断某个系统表是否允许进入逻辑复制系统表通路。
它应被以下模块共同使用：

1. WAL / relation logical logged 判断；
2. publication publishable 判断；
3. `pgoutput` 输出判断；
4. subscriber 目标关系打开检查。

## 5.3 当前白名单内容

当前仅返回真值的 relation 为：

```c
PublicationSyncRelationId
```

即：

```text
pg_publication_sync
```

## 5.4 后续扩展原则

如果未来要扩展其他系统表，必须逐表评估：

* 是否为 database-local catalog；
* 是否具备明确跨端语义；
* 是否需要 tuple 级路由；
* 是否允许映射到本地内建 catalog。

不得通过简单放宽判断函数来“一次性放开一批系统表”。

---

## 6. WAL 与逻辑日志抽取支持

## 6.1 目标

使白名单系统表在发生变更时，能够生成逻辑复制链路所需的 WAL 信息，从而具备被 logical decoding 读取的前提。

## 6.2 修改点

文件：

```c
src/include/utils/rel.h
```

关键判断：

```c
RelationIsLogicallyLogged(relation)
```

## 6.3 修改前语义

现有 PostgreSQL 对系统表通常默认不认为其属于逻辑复制数据来源。
即使 catalog 有 WAL，也不意味着这些变更会进入逻辑复制通路。

## 6.4 修改后语义

建议将 `RelationIsLogicallyLogged(relation)` 收敛为如下语义：

1. 普通用户表维持原有判断逻辑；
2. 非白名单系统表仍保持原生拒绝；
3. 只有白名单系统表才允许进入逻辑复制所需 WAL 信息生成路径。

可表达为：

* 普通关系：原逻辑不变
* 系统关系：

  * `IsLogicalRepSystemRelationOid(relid) == true` → 允许
  * 否则 → 拒绝

## 6.5 设计效果

这一层解决的是：

> 白名单系统表具备进入逻辑解码通路的 WAL 前提。

注意这只是必要条件，不是充分条件。
后续仍需要：

* publication 层放开；
* `pgoutput` 层输出支持；
* subscriber 层目标关系支持。

---

## 7. publication 层支持

文件：

```c
src/backend/catalog/pg_publication.c
```

本层的目标是：

1. 允许白名单系统表通过 publishable 判断；
2. 同时禁止用户以普通表方式显式管理 `pg_publication_sync`；
3. 通过 `pubddl` 隐式决定其是否属于 publication。

---

## 7.1 `is_publishable_class()` 放开

### 目标

让 `pg_publication_sync` 能通过 publication 关系集判断路径。

### 修改原则

`is_publishable_class()` 逻辑应调整为：

1. 普通用户关系维持原行为；
2. 非白名单系统表继续视为不可发布；
3. 白名单系统表可通过 publishable 判断；
4. 不因 `FirstNormalObjectId` 或 catalog 身份直接拒绝白名单系统表。

### 设计语义

这一步解决的是：

> relation 级别上，白名单系统表可以成为 publication 可见关系。

---

## 7.2 `check_publication_add_relation()` 禁止显式添加

### 目标

阻止用户执行：

```sql
ALTER PUBLICATION ... ADD TABLE pg_publication_sync;
```

### 修改原则

对于系统表：

1. 非白名单系统表：继续报错拒绝；
2. `pg_publication_sync`：也拒绝显式 `ADD TABLE`；
3. 报错信息中应明确提示：

   * 该关系由 `WITH (ddl=...)` 隐式管理；
   * 不允许作为普通关系显式加入 publication。

### 设计原因

如果允许显式加入，会导致：

* 与隐式成员机制冲突；
* 同一关系既来自 `pg_publication_rel` 又来自 `pubddl` 推导；
* 关系集语义不清晰。

因此必须明确：

> `pg_publication_sync` 只允许隐式成员语义，不允许显式成员语义。

---

## 8. `pg_publication_sync` 隐式成员设计

仍位于：

```c
src/backend/catalog/pg_publication.c
```

## 8.1 `GetRelationPublications(PublicationSyncRelationId)`

当某 publication 满足：

```text
pubddl > 0
```

则视为该 publication 隐式包含：

```text
pg_publication_sync
```

即该 publication 对该 relation 拥有 relation-level 可发布资格。

## 8.2 `GetPublicationRelations(pubid, ...)`

当某 publication 的 `pubddl > 0` 时，在获取 publication 关系集时自动补入：

```text
PublicationSyncRelationId
```

## 8.3 语义说明

这里要特别强调：

> relation 被 publication 隐式包含，仅表示该 publication 有资格发布 `pg_publication_sync` 上属于自己的消息行。

它**不表示**：

* 这张表上的所有行都属于该 publication；
* 任意消息行都可以从该 publication 发出。

真正的行归属仍由 `pfsyncpubid` 决定，并在 `pgoutput` 层进行二次过滤。

---

## 9. pgoutput 层支持

文件：

```c
src/backend/replication/pgoutput/pgoutput.c
```

这是设计2中最关键的一层，因为 `pg_publication_sync` 不是普通关系：

* 同一 relation 被多个 publication 隐式共享；
* 但消息行是 publication 私有的。

---

## 9.1 relation 级输出支持

### 目标

允许 `pg_publication_sync` 进入 `pgoutput` 输出路径。

### 修改原则

对于 relation：

1. 非白名单系统表：维持原生拒绝；
2. `pg_publication_sync`：允许继续走输出路径；
3. relation metadata（RELATION message）可正常发出。

### 设计语义

这一步只解决：

> `pg_publication_sync` 不再因是系统表而在输出层被整体拒绝。

---

## 9.2 动作范围限制

### 当前支持

* `INSERT`

### 当前不支持

* `UPDATE`
* `DELETE`
* `TRUNCATE`

### 设计原因

1. `pg_publication_sync` 本身为 append-only；
2. 设计2只需要支持当前实际会发生的动作；
3. 不应在输出层提前开放未来并不需要的 action，避免和表语义冲突。

因此建议明确：

> `pgoutput` 对 `pg_publication_sync` 当前仅支持 INSERT 变更发布。

---

## 9.3 tuple 级 publication 过滤

### 目标

防止 publication A 看到 publication B 的消息。

### 为什么普通 relation filtering 不够

如果只做 relation membership 判断：

* 只要 publication 开启了 `pubddl`
* 它就隐式包含 `pg_publication_sync`

但 `pg_publication_sync` 中的行是按 publication 粒度展开写入的。
所以如果没有额外行过滤，就会出现：

* 属于 publication B 的消息
* 也被 publication A 的订阅者收到

这会直接导致路由错误。

### 设计规则

对 `pg_publication_sync`，在 `pgoutput_change()` 或其等效路径中增加特殊逻辑：

1. 先按 relation membership 判断当前 publication 是否隐式包含该 relation；
2. 再读取 tuple 中的 `pfsyncpubid`；
3. 仅当该 `pfsyncpubid` 属于当前 walsender 请求输出的 publication 集时，才发送该 tuple。

### 语义表达

对于 `pg_publication_sync`：

```text
可发送 = relation 属于当前 publication
      AND tuple.pfsyncpubid 属于当前 publication 集
```

### 结果

* publication A 只发出 `pfsyncpubid = A` 的消息；
* publication B 只发出 `pfsyncpubid = B` 的消息；
* subscriber 无需再解析 `publication_list` 做交集判断。

这是设计2和设计3之间最重要的数据面契约之一。

---

## 10. subscriber 目标关系支持

文件：

```c
src/backend/replication/logical/relation.c
```

关键函数：

```c
logicalrep_rel_open()
```

目标是让 subscriber 在看到远端白名单系统表时，不再直接报错拒绝，而是允许把它映射到本地同名受控系统表。

---

## 10.1 打开检查放开

### 修改原则

当远端 relation 是系统表时：

1. 非白名单系统表：继续报错拒绝；
2. 白名单系统表：允许继续映射与打开。

### 设计语义

这一层解决：

> subscriber 不再因为 relation 属于 catalog 就一律拒绝。

---

## 10.2 本地目标关系约束

对于白名单系统表，不能像普通用户表那样：

* 映射到任意同名用户表；
* 或依赖用户自己手工建一个“接收表”。

必须要求：

1. 本地已存在对应内建 catalog relation；
2. 目标 relation 必须是本地真正的 `pg_publication_sync`；
3. 不允许普通用户关系替代。

### 设计原因

否则会带来以下风险：

* 误把普通表当成系统表复制目标；
* catalog 身份错乱；
* 升级或字段结构变化时失去受控边界；
* 逻辑复制链路被用户伪造对象污染。

因此本设计明确要求：

> 白名单系统表只能映射到本地同名内建 catalog relation。

---

## 10.3 结构兼容性说明

subscriber 侧还应默认要求：

* 本地 `pg_publication_sync` 结构已存在；
* 与远端协议字段兼容；
* 不做普通表那样的灵活列映射。

因为这是系统目录表，不是用户数据表。
其 schema 一致性应由系统版本与功能实现共同保证，而不是交给用户自定义。

---

## 11. 与设计3的边界

设计2在 subscriber 侧到此为止，只负责把远端系统表行安全送达本地同名系统表。

也就是说，设计2保证的是：

> “这条消息行能到达订阅端并写入本地 `pg_publication_sync`”

设计2**不负责**：

1. 解释 `pfsyncmsgtype = Q/A/D` 的业务含义；
2. 执行 `pfsyncddlsql`；
3. 更新 `pg_subscription_rel` 或其他订阅控制元数据；
4. leader apply worker 的控制逻辑；
5. 幂等和失败补偿策略。

这些全部属于设计3。

---

## 12. 风险与限制

## 12.1 白名单能力严格受限

当前仅支持：

* `pg_publication_sync`

这不是“系统表逻辑复制全面开放”，而是一个高度受控的单表特例能力。

---

## 12.2 不支持 shared catalog

当前设计严格限定为 database-local catalog relation。
不适用于：

* shared catalog
* cluster-wide metadata relation

---

## 12.3 当前仅支持 INSERT

由于 `pg_publication_sync` 为 append-only 消息表，当前逻辑复制通路只要求支持其 `INSERT`。

不为以下动作提供支持：

* UPDATE
* DELETE
* TRUNCATE

---

## 12.4 `pg_publication_sync` 需要特殊 tuple 级过滤

该 relation 的路由语义不同于普通用户表，不能简单依赖 relation membership。
后续如果引入新的白名单系统表，必须重新评估是否也需要类似 tuple 级路由机制。

---

## 12.5 与 catalog 现有假设的兼容风险

由于 PostgreSQL 原生逻辑复制广泛默认“系统表不复制”，因此实现时必须谨慎检查多层判断路径，避免遗漏仍然把系统表视为非法的分支。

设计文档层面已经明确：

* WAL 抽取
* publication 判断
* pgoutput
* subscriber 打开检查

都必须分别放开白名单，而不能假设改一处即可全链路生效。

---

## 13. 设计总结

设计2的本质可以概括为：

> **为 `pg_publication_sync` 打通一条受控、最小化、仅面向 append-only 消息复制的数据面通路。**

其关键收敛点包括：

1. 仅白名单系统表允许进入通路；
2. 仅限 database-local catalog；
3. `pg_publication_sync` 只允许隐式 publication 成员语义；
4. 不允许显式 `ADD TABLE pg_publication_sync`；
5. `pgoutput` 对该 relation 必须额外按 `pfsyncpubid` 做 tuple 级 publication 路由；
6. subscriber 只允许将其映射到本地同名内建 catalog；
7. 当前仅支持 `INSERT` 通路。

最终，设计2为设计3提供的契约是：

> 发布端写入 `pg_publication_sync` 的消息行，能够被正确路由、发布并送达到订阅端本地 `pg_publication_sync`。

---

## 14. 测试验证步骤

本节用于独立验证设计2的数据面通路，不覆盖设计3的自动执行逻辑。

### 14.1 验证目标

1. `pg_publication_sync` 可被 `WITH (ddl=...)` 的 publication 隐式纳入；
2. 禁止用户显式 `ADD TABLE pg_catalog.pg_publication_sync`；
3. `pgoutput` 对 `pg_publication_sync` 能按 `pfsyncpubid` 做 tuple 级路由过滤；
4. subscriber 侧可将该系统表变更写入本地 `pg_publication_sync`；
5. 当前仅验证 `INSERT` 路径。

### 14.2 前置条件

1. publisher、subscriber 两端均使用当前分支构建并重新 `initdb`；
2. publisher 开启 `wal_level=logical`；
3. 复制连接可用，且 subscriber 已具备基础复制权限；
4. 两端均存在内建系统表 `pg_catalog.pg_publication_sync`。

### 14.3 验证步骤

#### 步骤1：创建 publication 并确认隐式成员语义

发布端执行：

```sql
CREATE TABLE d2_base(id int primary key);

CREATE PUBLICATION d2_pub_a
FOR TABLE d2_base
WITH (ddl = 'table');

SELECT schemaname, tablename
  FROM pg_publication_tables
 WHERE pubname = 'd2_pub_a'
 ORDER BY 1, 2;
```

预期：

1. 结果包含 `public.d2_base`；
2. `pg_catalog.pg_publication_sync` 由 `ddl` 语义隐式纳入（无需显式 `ADD TABLE`）。

#### 步骤2：验证禁止显式添加系统消息表

发布端执行：

```sql
ALTER PUBLICATION d2_pub_a ADD TABLE pg_catalog.pg_publication_sync;
```

预期：报错拒绝，提示该系统表仅允许隐式成员语义。

#### 步骤3：构造双 publication，验证 `pfsyncpubid` 行级过滤

发布端执行：

```sql
CREATE PUBLICATION d2_pub_b
FOR TABLE d2_base
WITH (ddl = 'table');
```

订阅端执行（仅订阅 `d2_pub_a`）：

```sql
CREATE TABLE d2_base(id int primary key);

CREATE SUBSCRIPTION d2_sub_a
CONNECTION 'host=<publisher_host> port=<publisher_port> dbname=<publisher_db> user=<rep_user> password=<rep_password>'
PUBLICATION d2_pub_a
WITH (copy_data = false, ddl = 'table');
```

等待 apply 追平后，发布端执行：

```sql
CREATE TABLE d2_q_probe(id int);
```

订阅端执行：

```sql
SELECT count(*)
  FROM pg_catalog.pg_publication_sync
 WHERE pfsyncmsgtype = 'Q'
   AND position('CREATE TABLE d2_q_probe' in pfsyncddlsql) > 0;
```

预期：

1. 结果为 `1`；
2. 只收到 `d2_pub_a` 对应的行，不会收到 `d2_pub_b` 对应同语句行；
3. 证明 `pgoutput` 在 relation membership 之外还执行了 `pfsyncpubid` 级过滤。

#### 步骤4：验证订阅端系统表落地

订阅端执行：

```sql
SELECT pfsyncpubid, pfsyncobjid, pfsyncmsgtype, pfsyncddlsql
  FROM pg_catalog.pg_publication_sync
 WHERE position('CREATE TABLE d2_q_probe' in pfsyncddlsql) > 0
 ORDER BY pfsyncobjid;
```

预期：可见该消息行已落入订阅端本地 `pg_publication_sync`。

#### 步骤5：动作范围确认（本期仅 INSERT）

本期仅要求并验证 `pg_publication_sync` 的 `INSERT` 同步路径；
`UPDATE/DELETE/TRUNCATE` 不在设计2范围内。

### 14.4 单机双库调试注意事项（可选）

当 publisher/subscriber 在同一实例（例如 `pubdb/subdb`）时，`CREATE SUBSCRIPTION` 可能在创建逻辑槽阶段出现事务等待。
可采用以下顺序规避：

1. 在发布端预创建槽：`SELECT pg_create_logical_replication_slot('d2_sub_a','pgoutput');`
2. 在订阅端创建订阅时指定：`create_slot = false, slot_name = 'd2_sub_a'`

该步骤仅是本地调试技巧，不改变设计语义。

### 14.5 清理

发布端：

```sql
DROP TABLE IF EXISTS d2_q_probe;
DROP PUBLICATION IF EXISTS d2_pub_b;
DROP PUBLICATION IF EXISTS d2_pub_a;
DROP TABLE IF EXISTS d2_base;
```

订阅端：

```sql
DROP SUBSCRIPTION IF EXISTS d2_sub_a;
DROP TABLE IF EXISTS d2_base;
```
