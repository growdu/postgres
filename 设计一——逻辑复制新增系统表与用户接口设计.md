
# DDL 逻辑复制新增系统表与用户接口设计

## 1. 设计目标

为逻辑复制 DDL 同步能力补充完整的**目录模型**和**用户接口**，使系统具备以下基础能力：

1. 用户可通过 `CREATE/ALTER PUBLICATION` 显式声明 publication 允许发布的 DDL 类型集合；
2. 用户可通过 `CREATE/ALTER SUBSCRIPTION` 显式声明 subscription 允许接收的 DDL 类型集合；
3. 发布端可将命中的 DDL 事件转化为消息，持久写入系统目录表 `pg_publication_sync`；
4. 后续逻辑复制层可基于目录模型传输这些消息；
5. 后续订阅端 apply 层可基于统一消息格式进行解释执行。

本设计只定义：

* `pg_publication_sync` 的系统表模型；
* `pg_publication` / `pg_subscription` 的 catalog 扩展；
* SQL 参数扩展；
* DDL token 到 bitmask 的语义映射；
* 目录级生命周期与约束。

本设计不展开：

* WAL / logical decoding 细节；
* `pgoutput` 发送逻辑；
* 订阅端 `Q/A/D` 的 apply 执行逻辑。

---

## 2. 总体设计原则

### 2.1 三类 catalog 协同

本设计涉及三个 catalog：

1. `pg_publication`

   * 承载 publication 级 DDL 发布能力；
2. `pg_subscription`

   * 承载 subscription 级 DDL 接收能力；
3. `pg_publication_sync`

   * 承载发布端产生的 DDL 同步消息。

三者职责分别为：

* `pg_publication.pubddl`：定义“允许发布什么”
* `pg_subscription.subddl`：定义“允许接收什么”
* `pg_publication_sync`：记录“实际发生了什么”

---

### 2.2 双边过滤模型

DDL 同步能力采用双边控制：

* publication 侧：`pubddl`
* subscription 侧：`subddl`

最终某条 DDL 消息是否能进入订阅端控制面，取决于：

```text
publication 命中 AND subscription 命中
```

即：

* 发布端只发送自己允许发布的 DDL
* 订阅端只 apply 自己允许接收的 DDL

---

### 2.3 append-only 消息表模型

`pg_publication_sync` 是 append-only 消息表：

* 只 `INSERT`
* 不 `UPDATE`
* 不 `DELETE`

每条消息独立存在，用于：

* 复制
* 重试
* 审计
* 排障

---

### 2.4 publication 粒度展开

一条 DDL 若命中多个 publication，不聚合为一条共享消息，而是：

* 对每个 publication 单独写一条消息；
* 每条消息只归属一个 publication。

---

### 2.5 默认关闭

为保持对现有逻辑复制兼容：

* `pubddl` 默认 `0`
* `subddl` 默认 `0`

即未显式开启时：

* publication 默认不发布 DDL
* subscription 默认不接收 DDL

---

## 3. 扩展 `pg_publication`

## 3.1 目标

为 publication 增加 DDL 发布能力字段，使 publication 能声明：

> “这个 publication 允许发布哪些类别的 DDL”

---

## 3.2 表结构扩展

在 `pg_publication` 中新增列：

```c
pubddl int4
```

### 语义

`pubddl` 表示 publication 级 DDL 能力集合，采用 bitmask 编码。

* `0` 表示不发布任何 DDL
* 非 `0` 表示发布对应类别的 DDL

---

## 3.3 默认值

建议：

```c
pubddl BKI_DEFAULT(0)
```

含义：

* 默认关闭 DDL 发布
* 保持现有 publication 行为不变

---

## 3.4 使用场景

`pubddl` 主要用于：

1. DDL 捕获阶段判断是否需要写入 `pg_publication_sync`
2. 系统表逻辑复制层判断该 publication 是否隐式包含 `pg_publication_sync`
3. 订阅端 apply 前进行能力匹配

---

## 4. 扩展 `pg_subscription`

## 4.1 目标

为 subscription 增加 DDL 接收能力字段，使 subscription 能声明：

> “这个 subscription 允许接收哪些类别的 DDL”

---

## 4.2 表结构扩展

在 `pg_subscription` 中新增列：

```c
subddl int4
```

### 语义

`subddl` 表示 subscription 允许接收的 DDL 能力集合，采用 bitmask 编码。

* `0` 表示不接收任何 DDL
* 非 `0` 表示接收对应类别的 DDL

---

## 4.3 默认值

建议：

```c
subddl BKI_DEFAULT(0)
```

含义：

* 默认关闭 DDL 接收
* 保持现有 subscription 行为不变

---

## 4.4 使用场景

`subddl` 主要用于：

1. 订阅端 apply 前过滤消息；
2. 判断某条 `Q/A/D` 消息是否允许进入控制面执行；
3. 避免订阅端收到但不应处理的 DDL 类型进入 apply。

---

## 5. DDL token 设计

## 5.1 用户接口支持的 token

`ddl='...'` 支持如下 token：

* `table`
* `index`
* `trigger`
* `view`
* `rule`
* `schema`
* `function`
* `type`
* `domain`
* `extension`
* `all`

---

## 5.2 token 语义

这些 token 表示**DDL 类别**，而不是消息类型。

例如：

* `table`：表相关 DDL
* `index`：索引相关 DDL
* `function`：函数相关 DDL
* `schema`：schema 相关 DDL

`Q/A/D` 只是消息主分类，不由用户直接配置。

---

## 5.3 `all` 语义

`all` 表示启用所有当前支持的 DDL 类别。

建议在解析时展开为所有已定义 bit 的并集。

---

## 5.4 token 解析规则

建议：

1. 大小写不敏感
2. 允许逗号分隔多个 token
3. 去除空白
4. 重复 token 自动去重
5. 非法 token 直接报错

例如：

```sql
WITH (ddl = 'table,index,function')
```

应解析为对应 bitmask 的按位或结果。

---

## 6. SQL 用户接口扩展

## 6.1 扩展 `CREATE PUBLICATION`

新增参数：

```sql
CREATE PUBLICATION name
...
WITH (ddl = '...')
```

### 示例

```sql
CREATE PUBLICATION pub_ddl
FOR TABLE t1, t2
WITH (publish = 'insert,update,delete', ddl = 'table,index');
```

---

## 6.2 扩展 `ALTER PUBLICATION`

新增参数：

```sql
ALTER PUBLICATION name
SET (ddl = '...')
```

### 示例

```sql
ALTER PUBLICATION pub_ddl
SET (ddl = 'table,index,function');
```

---

## 6.3 扩展 `CREATE SUBSCRIPTION`

新增参数：

```sql
CREATE SUBSCRIPTION name
CONNECTION '...'
PUBLICATION pub_ddl
WITH (ddl = '...')
```

### 示例

```sql
CREATE SUBSCRIPTION sub_ddl
CONNECTION '...'
PUBLICATION pub_ddl
WITH (copy_data = false, ddl = 'table,index');
```

---

## 6.4 扩展 `ALTER SUBSCRIPTION`

新增参数：

```sql
ALTER SUBSCRIPTION name
SET (ddl = '...')
```

### 示例

```sql
ALTER SUBSCRIPTION sub_ddl
SET (ddl = 'table,function');
```

---

## 6.5 默认行为

若用户未指定 `ddl`：

* publication：`pubddl = 0`
* subscription：`subddl = 0`

即默认不启用 DDL 同步。

---

## 7. `pg_publication_sync` 系统表设计

## 7.1 目标

新增系统目录表 `pg_publication_sync`，用于承载发布端产生的 DDL 同步消息。

该表只表达：

> 某个 publication 下，在某个时间点发生了一条 DDL 同步消息。

---

## 7.2 基本信息

定义文件：

```c
src/include/catalog/pg_publication_sync.h
```

关系 OID：

```c
9352
```

关系名：

```c
pg_publication_sync
```

---

## 7.3 基本定位

`pg_publication_sync` 是：

* append-only
* 只存消息行
* 不存配置行
* 不存状态行
* 不存执行结果行

当前只存三类消息：

* `Q`
* `A`
* `D`

---

## 8. `pg_publication_sync` 字段设计

## 8.1 路由与元信息

### `pfsyncpubid`

* 类型：`Oid`
* 含义：消息所属 publication（发布端 OID）

说明：

* 一条消息只归属一个 publication
* 是消息路由的核心字段

---

### `pfsyncobjid`

* 类型：建议 `Oid` 或 `int8`
* 含义：消息事件 ID

说明：

* 当前语义为**消息 ID**
* 不表示数据库对象 OID
* 若允许改名，更建议命名为 `pfsyncmsgid`

---

### `pfsyncddl`

* 类型：`int4`
* 含义：DDL 类型 bitmask

说明：

* 对应用户接口里 `ddl='...'` 的类别集合
* `Q/A/D` 消息都携带该类别信息

---

### `pfsyncenabled`

* 类型：`bool`
* 含义：保留字段
* 当前固定写入 `true`

---

### `pfsynclsn`

* 类型：`pg_lsn`
* 含义：消息写入时记录的 LSN

---

### `pfsyncts`

* 类型：`timestamptz`
* 含义：消息写入时间

---

## 8.2 消息载荷

### `pfsyncmsgtype`

* 类型：建议 `"char"`
* 当前取值：

  * `Q`
  * `A`
  * `D`

---

### `pfsynctargettable`

* 类型：`text`
* 含义：目标对象文本标识

---

### `pfsyncddlsql`

* 类型：`text`
* 含义：DDL SQL 文本

约束建议：

* `Q` 必填
* `A/D` 可空

---

### `pfsyncsearchpath`

* 类型：`text`
* 含义：捕获消息时的 `search_path`

约束建议：

* `Q` 必填
* `A/D` 可空

---

### `pfsyncpublicationlist`

* 类型：`text`
* 含义：保留字段

说明：

* 当前不参与订阅端决策
* 当前不参与路由
* 仅用于调试/兼容预留

---

### `pfsyncextra`

* 类型：`text`
* 含义：扩展附加信息

---

## 9. `pg_publication_sync` 行约束

## 9.1 只允许消息行

不允许写入：

* publication 配置行
* publication 默认能力行
* 对象状态行
* 执行结果行

---

## 9.2 消息类型约束

当前只支持：

* `Q`
* `A`
* `D`

其他值非法。

---

## 9.3 必填约束建议

### `Q` 消息必须具备

* `pfsyncpubid`
* `pfsyncmsgtype='Q'`
* `pfsyncddl`
* `pfsyncddlsql`
* `pfsyncsearchpath`
* `pfsynclsn`
* `pfsyncts`

### `A/D` 消息必须具备

* `pfsyncpubid`
* `pfsyncmsgtype='A'` 或 `D`
* `pfsyncddl`
* `pfsynctargettable`
* `pfsynclsn`
* `pfsyncts`

---

## 10. 唯一性与索引设计

## 10.1 不使用行级 OID

该表不通过行级 `oid` 标识记录。

---

## 10.2 唯一性建议

建议唯一索引：

```text
(pfsyncpubid, pfsyncobjid)
```

含义：

* 在单个 publication 作用域下，消息事件 ID 唯一。

若后续消息 ID 采用全局唯一分配机制，也可调整为：

```text
(pfsyncobjid)
```

---

## 10.3 普通索引建议

### publication 维度索引

```text
(pfsyncpubid)
```

### LSN 维度索引

```text
(pfsynclsn)
```

### 消息类型索引（可选）

```text
(pfsyncmsgtype)
```

---

## 11. 写入规则

## 11.1 总体原则

任何进入 DDL 同步链路的发布端事实，都必须写入一条新的消息记录。

不做 upsert，不做 update 覆盖。

---

## 11.2 `Q` 消息写入

当发布端捕获普通 DDL 并命中某 publication 时，写入一条 `Q` 消息。

如果同一条 DDL 命中多个 publication：

* 对每个 publication 各写一条独立消息；
* 不聚合写入。

---

## 11.3 `A` 消息写入

当 publication 成员新增时，写入 `A` 消息。

典型场景：

* `ALTER PUBLICATION ... ADD TABLE`

---

## 11.4 `D` 消息写入

当 publication 成员移除时，写入 `D` 消息。

典型场景：

* `ALTER PUBLICATION ... DROP TABLE`
* `DROP PUBLICATION` 展开后的对象退出消息

---

## 12. 生命周期设计

## 12.1 持久保留

`pg_publication_sync` 默认持久保留，用于：

* 复制
* 重试
* 审计
* 排障

---

## 12.2 publication 删除

publication 删除时：

* 不反向清理历史消息
* 只新增必要的 `D` 消息
* 旧消息继续保留

---

## 12.3 清理函数

提供：

```sql
select pg_publication_sync_prune();
```

### 裁剪原则

同时满足以下条件才允许删除：

1. `pfsynclsn` 早于安全 LSN 下界
2. `pfsyncts` 早于保留时间窗口

即采用：

* 逻辑槽最小 restart LSN
* 时间窗口

的联合约束裁剪历史消息。

---

## 13. 用户接口与系统表的关系

这部分是本设计最关键的贯通点。

### 13.1 `pubddl` 的作用

`pg_publication.pubddl` 定义：

> 某个 publication 理论上允许发布哪些 DDL 类别

它控制“有没有资格生成消息”。

---

### 13.2 `subddl` 的作用

`pg_subscription.subddl` 定义：

> 某个 subscription 理论上允许接收哪些 DDL 类别

它控制“收到消息后是否有资格进入 apply”。

---

### 13.3 `pg_publication_sync` 的作用

`pg_publication_sync` 记录：

> 某个 publication 下，实际发生了一条什么 DDL 消息

它是“实际事实表”。

---

### 13.4 三者关系总结

可以总结成：

* `pubddl`：发布能力声明
* `subddl`：接收能力声明
* `pg_publication_sync`：消息事实记录

---

## 14. 设计总结

本设计统一完成了以下两类扩展：

### 14.1 用户接口与 catalog 能力扩展

* `pg_publication.pubddl`
* `pg_subscription.subddl`
* `CREATE/ALTER PUBLICATION ... WITH (ddl='...')`
* `CREATE/ALTER SUBSCRIPTION ... WITH (ddl='...')`

### 14.2 消息系统表扩展

* 新增 `pg_publication_sync`
* 采用 append-only 消息表模型
* 只存 `Q/A/D`
* 按 publication 粒度展开
* 默认持久保留

因此，这份设计的核心可以概括为：

> 用 `pubddl/subddl` 解决“用户如何声明 DDL 能力”，
> 用 `pg_publication_sync` 解决“系统如何持久化实际发生的 DDL 同步消息”。
