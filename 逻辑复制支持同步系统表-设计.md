# 设计2：逻辑复制支持同步系统表

更新时间：2026-04-17

对应测试文档：  
[逻辑复制支持同步系统表-测试验证步骤.md](/Users/growduduan/cwork/postgresql-2/逻辑复制支持同步系统表-测试验证步骤.md)

## 1. 目标

让白名单系统表具备完整逻辑复制通路能力：

1. 发布端可产生日志并发布变更。
2. pgoutput 可输出变更。
3. 订阅端可将该系统表作为合法复制目标。

本设计不负责 DDL 自动执行语义（由设计3负责）。

## 2. 白名单策略

核心原则：只放开明确允许的系统表，不放开全量 catalog。

实现：

1. `src/backend/replication/logical/logicalsysrel.c`
2. `IsLogicalRepSystemRelationOid(Oid relid)`

当前白名单仅包含：

* `PublicationSyncRelationId`（`pg_publication_sync`）

## 3. WAL 层放开

文件：`src/include/utils/rel.h`。

`RelationIsLogicallyLogged(relation)` 已改为：

1. 普通用户表保持原行为。
2. 系统表仅当在白名单中才允许逻辑日志抽取。

这一步解决“系统表默认不产出逻辑复制所需 WAL 信息”的问题。

## 4. publication 层放开

文件：`src/backend/catalog/pg_publication.c`。

关键点：

1. `check_publication_add_relation()`：
   * 非白名单系统表仍拒绝；
   * 白名单系统表可加入 publication。
2. `is_publishable_class()`：
   * 对白名单系统表放开 publishable 判断；
   * 兼容 `FirstNormalObjectId` 限制。

## 5. `pg_publication_sync` 的隐式成员关系

仍在 `pg_publication.c` 中：

1. `GetRelationPublications(PublicationSyncRelationId)`：
   * 当 `pubddl > 0` 时，publication 视为隐式包含 `pg_publication_sync`。
2. `GetPublicationRelations(pubid, ...)`：
   * 当 publication 配置了 `WITH (ddl=...)`，自动返回 `PublicationSyncRelationId`。

含义：不需要显式 `ADD TABLE pg_publication_sync`。

## 6. pgoutput 层支持

文件：`src/backend/replication/pgoutput/pgoutput.c`。

针对 `PublicationSyncRelationId`：

1. 直接视为可发布元数据关系。
2. 行级动作统一允许（insert/update/delete/truncate 不受普通 publish action 约束）。

## 7. subscriber 目标关系支持

文件：`src/backend/replication/logical/relation.c`。

`logicalrep_rel_open()` 增加检查：

1. 若本地关系是系统表且不在白名单，报错拒绝。
2. 白名单系统表允许继续走映射与 apply 流程。

## 8. 与设计3的边界

本设计只保证“系统表变更能到达订阅端并写入系统表”。

以下不在本设计范围：

1. `Q/A/D` 业务语义解释；
2. DDL SQL 执行；
3. subscription 成员映射维护。

这些由设计3处理。

## 9. 风险与限制

1. 只支持白名单系统表。
2. 不建议扩展到 shared catalog。
3. 非白名单系统表保持原生拒绝行为。
