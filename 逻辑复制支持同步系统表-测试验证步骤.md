# 测试2：逻辑复制支持同步系统表验证步骤

对应设计文档：  
[逻辑复制支持同步系统表-设计.md](/Users/growduduan/cwork/postgresql-2/逻辑复制支持同步系统表-设计.md)

## 1. 目标

独立验证“系统表同步能力”本身：

1. `pg_publication_sync` 能被 publication 隐式纳入；
2. 发布端变更可经逻辑复制同步到订阅端；
3. 不依赖 DDL 自动 apply 行为（仅验证系统表行复制）。

## 2. 前置条件

1. 两节点：publisher、subscriber。
2. `wal_level=logical`，复制账号可用。
3. 两端均使用当前分支构建并重新 `initdb`。

## 3. 验证步骤

### 3.1 发布端创建 publication（启用 ddl）

```sql
CREATE TABLE sysrel_base(id int primary key);

CREATE PUBLICATION pub_sysrel_data
FOR TABLE sysrel_base
WITH (ddl = 'table');

SELECT schemaname, tablename
  FROM pg_publication_tables
 WHERE pubname = 'pub_sysrel_data'
 ORDER BY 1, 2;
```

预期：

1. 有 `public.sysrel_base`。
2. 隐式包含 `pg_catalog.pg_publication_sync`（无需显式 `ADD TABLE`）。

### 3.2 订阅端建立订阅

```sql
CREATE TABLE sysrel_base(id int primary key);

CREATE SUBSCRIPTION sub_sysrel_data
CONNECTION 'host=... port=... dbname=... user=... password=...'
PUBLICATION pub_sysrel_data
WITH (copy_data = false, ddl = 'table');
```

等待 apply 追平后继续。

### 3.3 发布端修改 publication 的 ddl 配置

```sql
ALTER PUBLICATION pub_sysrel_data
SET (ddl = 'table,index');
```

### 3.4 订阅端检查 `pg_publication_sync` 的 `p` 行同步

```sql
SELECT p.pubname, s.pfsynckind, s.pfsyncenabled, s.pfsyncddl
  FROM pg_publication_sync s
  JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE p.pubname = 'pub_sysrel_data'
   AND s.pfsynckind = 'p';
```

预期：

1. 存在对应 `p` 行。
2. `pfsyncddl` 为修改后的 mask（包含 `table,index`）。
3. 说明系统表行变更已通过逻辑复制同步到订阅端。

## 4. 负向校验（可选）

发布端执行：

```sql
CREATE PUBLICATION pub_bad_sys FOR TABLE pg_catalog.pg_class;
```

预期：

1. 报错拒绝（非白名单系统表不可复制）。

## 5. 清理

发布端：

```sql
DROP PUBLICATION IF EXISTS pub_sysrel_data;
DROP TABLE IF EXISTS sysrel_base;
```

订阅端：

```sql
DROP SUBSCRIPTION IF EXISTS sub_sysrel_data;
DROP TABLE IF EXISTS sysrel_base;
```

