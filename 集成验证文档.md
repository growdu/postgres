# 集成验证文档

## 1. 目标

验证设计3与设计4的端到端链路是否成立：

- 设计3：`pg_publication_sync` 消息到达后可自动执行（Q/A/D）
- 设计4：消息先落 `pg_publication_recv`，再按状态机推进（R -> A -> S）

## 2. 代码基线

- 设计3提交：`20597767124`（Q/A/D 直接 apply）
- 设计4提交：`e83bbe544a9`（新增 `pg_publication_recv` 与状态流）

## 3. 环境准备

```bash
rtk make -s -j8
rtk make -s install prefix=/Users/growduduan/cwork/postgresql-3/tmp_install/usr/local/pgsql
```

环境变量：

- `PGROOT=/Users/growduduan/cwork/postgresql-3/tmp_install/usr/local/pgsql`
- `DYLD_LIBRARY_PATH=$PGROOT/lib`

macOS 注意事项（放行方式）：

当 `libpqwalreceiver` 仍引用 `/usr/local/pgsql/lib/libpq.5.dylib` 时，可执行：

```bash
rtk proxy /bin/zsh -lc "install_name_tool -change /usr/local/pgsql/lib/libpq.5.dylib @loader_path/libpq.5.dylib /Users/growduduan/cwork/postgresql-3/tmp_install/usr/local/pgsql/lib/libpqwalreceiver.dylib"
```

## 4. 验证场景

### 4.1 Q 消息场景

1. 发布端创建 `FOR ALL TABLES` publication（`ddl='table'`）。
2. 订阅端创建 subscription（`ddl='table'`，`copy_data=false`）。
3. 发布端执行 `CREATE TABLE d4_auto_q(...)`。
4. 校验：
- 订阅端 `to_regclass('public.d4_auto_q')` 不为 `NULL`
- `pg_publication_recv` 中存在 `msgtype='Q'` 且 `pfrecvstate='S'` 的记录

### 4.2 A/D 消息场景

1. 发布端建立 `d4_pub_ad`，初始仅含 `d4_anchor_ad`。
2. 执行 `ALTER PUBLICATION ... ADD TABLE d4_ad_member`。
3. 校验订阅端 `pg_subscription_rel` 出现目标表映射。
4. 执行 `ALTER PUBLICATION ... DROP TABLE d4_ad_member`。
5. 校验订阅端 `pg_subscription_rel` 中该映射被移除。
6. 校验 `pg_publication_recv`：
- `msgtype='A'` 对应记录状态为 `S`
- `msgtype='D'` 对应记录状态为 `S`

## 5. 最近一次实测结果

本次实测使用端口 `55489`，关键结果如下：

- `Q_TABLE_ON_SUB=1`
- `Q_RECV_ROWS=1`
- `Q_RECV_SUCCESS=1`
- `AD_AFTER_ADD=1`
- `AD_AFTER_DROP=0`
- `A_RECV_SUCCESS=1`
- `D_RECV_SUCCESS=1`

结论：设计3与设计4主链路验证通过。

## 6. 清理步骤

验证完成后建议执行以下清理：

1. 停止临时实例：`pg_ctl -D <datadir> -m immediate stop`
2. 删除临时 datadir：`/tmp/pg_d3_diag.*`、`/tmp/pg_d4_validate.*`
3. 删除临时脚本与日志：`/tmp/validate_design*_pg3*.sh`、`/tmp/pg_d3_*.log`
4. 清理仓库临时输出：`.DS_Store`、`src/test/regress/regression.diffs`、`src/test/regress/regression.out`
