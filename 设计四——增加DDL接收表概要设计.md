
# 设计4：`pg_publication_recv`（接收表与增强能力）

---

# 1. 设计目标

在设计3最小闭环的基础上，引入订阅端接收表 `pg_publication_recv`，将“消息接收”和“消息执行”从同步瞬时行为升级为**可持久化、可跟踪、可重试**的控制面模型。

设计4要解决的问题包括：

1. 订阅端收到 DDL 消息后，先可靠落地，再执行；
2. 区分“收到成功”和“执行成功”；
3. 对执行失败的消息进行状态记录和后续重试；
4. 支持运维审计与故障排查；
5. 为级联订阅/循环订阅提供来源识别基础；
6. 为多来源接收提供统一承载表；
7. 为 DDL / DML 顺序控制提供增强机制（DDL barrier）。

---

# 2. 设计范围

## 2.1 包含内容

* 新增系统表 `pg_publication_recv`
* 接收表字段设计
* 接收状态机设计
* 来源标识设计
* 远端消息标识设计
* 执行结果与错误记录设计
* 重试模型
* DDL barrier 设计
* 多来源接收模型
* 对象来源唯一性约束
* 级联/循环订阅增强基础

## 2.2 不包含内容

* `pg_publication_sync` 的发布端表结构
* `pubddl` / `subddl` 用户接口
* 系统表逻辑复制白名单与发送通路
* 直接执行版（设计3）的最小闭环实现

---

# 3. 设计定位

`pg_publication_recv` 被定义为：

> **订阅端本地的 DDL 消息接收与执行状态表**

它与 `pg_publication_sync` 的关系如下：

| 表                     | 角色       | 写入方   | 是否可更新          |
| --------------------- | -------- | ----- | -------------- |
| `pg_publication_sync` | 发布消息表    | 发布端本地 | 否（append-only） |
| `pg_publication_recv` | 接收与执行状态表 | 订阅端本地 | 是              |

换句话说：

* `sync` 表回答：**“本地产生了什么消息，准备向外发布？”**
* `recv` 表回答：**“我从远端收到了什么消息，它执行到哪一步了？”**

---

# 4. 为什么需要 `pg_publication_recv`

设计4引入 `recv` 表的根本原因是：设计3虽然能跑通，但不够稳。

设计3中的“收到即执行”存在以下不足：

1. 无法区分消息已收到还是已执行；
2. DDL 执行失败后没有可靠状态沉淀；
3. 无法方便地做自动或人工重试；
4. 无法审计和排查历史失败消息；
5. 无法支持级联/循环订阅的来源跟踪；
6. 多来源场景下缺乏统一接收承载层；
7. 若未来要做调度控制，缺少可管理的队列表。

因此设计4的基本思想是：

> **先接收落地，再受控执行。**

---

# 5. 设计原则

---

## 5.1 接收与执行分离

设计4明确将控制面拆成两步：

1. **接收**

   * 消息到达订阅端后，先写入 `pg_publication_recv`
2. **执行**

   * leader apply worker 再从 `recv` 表中取消息执行

这样可以把：

* 数据到达
* 业务执行

明确区分开。

---

## 5.2 先持久化，再执行

任何远端 DDL 消息到达本地后，都应先可靠写入 `pg_publication_recv`，然后才能进入执行阶段。

这意味着：

* 接收成功 ≠ 执行成功
* 写入 `recv` 成功后，即使 worker 崩溃，消息也不会丢

---

## 5.3 来源可识别

由于未来需要支持：

* 多 subscription
* 多发布端
* 级联订阅
* 循环订阅控制

因此 `recv` 表必须记录**消息来源信息**，不能只记录“消息内容”。

---

## 5.4 状态可追踪

`recv` 表不能只是消息副本，还必须是**执行状态表**。
至少要能区分：

* 已收到未执行
* 正在执行
* 执行成功
* 执行失败

---

## 5.5 顺序优先于吞吐

DDL 同步的正确性优先于吞吐。
因此设计4中的调度必须服从：

* 来源流内顺序
* 前驱消息先完成
* 失败阻塞后续执行

而不是追求并发执行更多消息。

---

## 5.6 与 DML 语义保持一致

由于 DDL 使用与 DML 相同的 publication/subscription 用户接口，设计4必须尽量对齐 DML 的核心语义模型：

1. 单来源流顺序推进
2. 无多源冲突自动解决
3. 单对象应有唯一 schema 来源
4. 错误默认阻塞后续执行

---

# 6. 表定义目标

新增系统表：

```c
src/include/catalog/pg_publication_recv.h
```

建议关系名：

```text
pg_publication_recv
```

该表不再是 append-only，而是：

* 初次写入时 insert
* 执行阶段会 update 状态
* 必要时支持 prune 或归档

---

# 7. 字段设计

建议将字段分为五组：

1. 来源信息
2. 远端消息身份
3. 消息载荷
4. 本地执行状态
5. 审计与重试信息

---

## 7.1 来源信息字段

### `pfrecvsubid`

* 类型：`Oid`
* 含义：本地 subscription OID

#### 作用

表示该消息是通过哪个本地 subscription 收到的。

这是第一版最重要的“来源流”字段。
设计4建议将“来源流”首先定义为：

> **一个 subscription**

---

### `pfrecvorigin`

* 类型：`text`
* 含义：远端来源节点标识

#### 作用

用于标记该消息来自哪个远端节点/来源域。
这是级联/循环订阅场景中的关键字段。

它不能依赖远端本地 OID，而应是可跨节点解释的稳定标识，例如：

* 节点名
* 系统标识字符串
* 发布端实例标识

---

### `pfrecvpubname`

* 类型：`text`
* 含义：远端 publication 名称

#### 作用

记录远端 publication 的文本身份。
相比远端 `pubid`，名称在本地更可解释，也更适合审计与级联诊断。

---

## 7.2 远端消息身份字段

### `pfrecvremote_msgid`

* 类型：建议 `int8` 或与 `sync` 表消息 ID 同类型
* 含义：远端消息事件 ID

#### 作用

标识这条消息在远端来源流中的消息编号。

---

### `pfrecvremote_lsn`

* 类型：`pg_lsn`
* 含义：远端消息顺序位点

#### 作用

这是设计4中的核心顺序控制字段。
它不只是审计字段，更是：

> **DDL 与 DML 顺序协调的基础排序键**

---

### `pfrecvremote_ts`

* 类型：`timestamptz`
* 含义：远端生成时间

#### 作用

用于审计、诊断和对账。

---

## 7.3 消息载荷字段

### `pfrecvmsgtype`

* 类型：`char`
* 取值：

  * `Q`
  * `A`
  * `D`

---

### `pfrecvddl`

* 类型：`int4`
* 含义：DDL 类别 bitmask

---

### `pfrecvtargettable`

* 类型：`text`
* 含义：目标对象标识

---

### `pfrecvddlsql`

* 类型：`text`
* 含义：DDL SQL 文本

---

### `pfrecvsearchpath`

* 类型：`text`
* 含义：执行上下文 search_path

---

### `pfrecvextra`

* 类型：`text`
* 含义：扩展附加信息

---

## 7.4 本地执行状态字段

### `pfrecvstate`

* 类型：`char`
* 建议取值：

| 值   | 含义                     |
| --- | ---------------------- |
| `R` | Received，已收到未执行        |
| `A` | Applying，正在执行          |
| `S` | Success，执行成功           |
| `F` | Failed，执行失败            |
| `K` | Skipped，跳过             |
| `D` | Discarded/Dead，废弃或不再处理 |

第一版最少需要：

* `R`
* `A`
* `S`
* `F`

---

### `pfrecvrecvts`

* 类型：`timestamptz`
* 含义：本地接收时间

---

### `pfrecvapplyts`

* 类型：`timestamptz`
* 含义：最后一次开始执行时间

---

### `pfrecvfinishts`

* 类型：`timestamptz`
* 含义：最后一次执行结束时间

---

## 7.5 错误与重试字段

### `pfretry_count`

* 类型：`int4`
* 含义：已重试次数

---

### `pfrecverrcode`

* 类型：`text`
* 含义：错误码

---

### `pfrecverrmsg`

* 类型：`text`
* 含义：错误消息文本

---

### `pfrecverrdetail`

* 类型：`text`
* 含义：错误详细信息（可选）

---

### `pfnext_retry_ts`

* 类型：`timestamptz`
* 含义：下一次可重试时间（可选）

---

# 8. 唯一性与索引设计

---

## 8.1 唯一性原则

由于一个本地数据库可以通过多个 subscription 从多个远端接收消息，
`recv` 表的唯一性必须带来源维度。

建议唯一键至少包含：

* `pfrecvsubid`
* `pfrecvremote_lsn`
* `pfrecvremote_msgid`

即：

```text
(pfrecvsubid, pfrecvremote_lsn, pfrecvremote_msgid)
```

这表示：

> 在同一个 subscription 来源流中，一条远端消息只能被接收一次。

---

## 8.2 普通索引建议

### 1. 按执行状态查询

```text
(pfrecvsubid, pfrecvstate, pfrecvremote_lsn)
```

用于 leader apply worker 扫描待执行消息。

### 2. 按来源诊断

```text
(pfrecvorigin, pfrecvpubname)
```

### 3. 按接收时间或执行时间

```text
(pfrecvrecvts)
(pfrecvfinishts)
```

---

# 9. 接收流程设计

---

## 9.1 接收入口

当设计2的数据面将远端 `pg_publication_sync` 消息送到订阅端后，设计4不再“直接执行”，而是：

1. 解析消息；
2. 写入 `pg_publication_recv`；
3. 置 `pfrecvstate='R'`

---

## 9.2 接收语义

写入 `recv` 表仅表示：

> **我收到了这条远端消息**

不表示：

* 已成功执行
* 已成功应用到本地 schema
* 已可继续后续消息

---

## 9.3 写入失败语义

若写入 `pg_publication_recv` 失败，则整个消息处理失败。
因为设计4的核心就是“先持久化，再执行”。

---

# 10. 执行流程设计

---

## 10.1 执行者

仍然只允许：

> **leader apply worker**

执行 `recv` 表中的 DDL 消息。

不允许 parallel apply worker 执行 DDL。

---

## 10.2 调度入口

可设计为类似：

```c
maybe_apply_publication_recv_message()
```

由 leader apply worker 周期性扫描：

* `state='R'`
* 或 `state='F'` 且满足重试条件

的消息。

---

## 10.3 执行前状态迁移

执行前必须先把消息状态从：

```text
R -> A
```

并记录：

* `pfrecvapplyts`

这样可以避免并发重复执行。

---

## 10.4 执行后状态迁移

### 成功

```text
A -> S
```

更新：

* `pfrecvfinishts`

### 失败

```text
A -> F
```

更新：

* `pfrecvfinishts`
* `pfretry_count += 1`
* 错误信息字段

---

# 11. 顺序保证设计

这是设计4最关键的部分之一。

---

## 11.1 为什么设计4比设计3更复杂

设计3中，DDL 与 DML 都在 apply 主流中直接执行，因此天然复用原生顺序。

设计4中，DDL 先落 `recv`，再执行，这就引入了“两阶段”：

1. 接收顺序
2. 执行顺序

它们不天然一致，所以必须额外保证顺序。

---

## 11.2 来源流顺序

设计4第一版建议：

> **按 subscription 作为来源流**

也就是说：

* 每个 `pfrecvsubid` 内部，消息必须严格按远端顺序推进

排序键建议为：

```text
ORDER BY pfrecvremote_lsn, pfrecvremote_msgid
```

---

## 11.3 串行调度保证

leader apply worker 必须满足：

1. 同一 `subid` 只由一个执行者消费
2. 每次只取该 `subid` 下**最小未完成**的一条
3. 不允许跳过前驱消息

也就是说，不能：

* 先执行后面的
* 前面失败了还继续执行后面的

---

## 11.4 失败阻塞原则

如果某条消息执行失败，则同一来源流中顺序更晚的消息默认不应继续执行。

因为 DDL 往往存在强依赖：

* 表创建失败，后续索引创建不应继续
* 列新增失败，后续 DML 可能全部不成立

因此设计4默认采用：

> **失败阻塞后续**

---

# 12. DDL 与 DML 顺序协调（DDL Barrier）

这是设计4必须额外解决的问题。

---

## 12.1 问题来源

由于 DDL 先落 `recv`，而 DML 仍可能按原生 apply 主流继续推进，就会产生风险：

* DDL 已收到但未执行
* 后续依赖该 DDL 的 DML 已经开始 apply

这会导致顺序错乱。

---

## 12.2 核心机制：DDL Barrier

设计4必须引入：

> **DDL barrier（DDL 顺序栅栏）**

含义是：

* 对于当前即将 apply 的 DML
* 如果存在同一 subscription 中顺序更早、但尚未成功完成的 DDL
* 则该 DML 必须被阻塞
* 必须先完成这些 DDL

---

## 12.3 Barrier 规则

对于当前 DML 的来源顺序键 `current_dml_lsn`：

若存在：

* `pfrecvsubid = 当前 subscription`
* `pfrecvstate != 'S'`
* `pfrecvremote_lsn < current_dml_lsn`

的 DDL 消息，则：

* 不允许继续 apply 该 DML
* 必须先处理这些更早 DDL

---

## 12.4 结果

这样可以恢复与设计3相同的核心语义：

> **DDL 与 DML 的最终执行顺序仍与发布端来源流一致。**

---

# 13. 多来源接收设计

---

## 13.1 是否允许同一数据库接收多个来源

允许。
一个本地数据库可以有多个 subscription，对应多个远端发布端。

因此：

* 多个来源可以共用同一张 `pg_publication_recv` 表

---

## 13.2 为什么可以共用同一张表

因为 `recv` 表已经包含了：

* `pfrecvsubid`
* `pfrecvorigin`
* `pfrecvpubname`
* 远端消息顺序键

这些字段足以区分不同来源。

所以不需要：

* 每个来源一张接收表
* 每个 subscription 单独建 catalog

---

# 14. 对象来源唯一性约束

这是设计4必须写死的约束。

---

## 14.1 为什么必须限制

即使 DML 多源同表都没有清晰的一致性语义，
DDL 多源同表更危险，因为它会直接破坏 schema。

例如两个不同来源同时对同一张本地表做：

* 增列
* 删列
* 改约束

本地无法有明确仲裁规则。

---

## 14.2 建议约束

第一版建议直接定义为：

> **同一张本地表只允许由一个 subscription 管理 DDL 自动同步。**

换句话说：

* 多个来源可以存在
* 但不能同时控制同一张表的 schema 演化

---

## 14.3 检查时机

建议在 `A` 消息处理阶段检查：

* 当某表准备加入某 subscription 的 DDL 管理范围时
* 检查它是否已被其他 subscription 占用
* 若是，则报错拒绝

---

# 15. 级联 / 循环订阅支持基础

这是设计4相对设计3最大的扩展价值之一。

---

## 15.1 为什么设计3不适合级联

设计3中：

* 收到就执行
* 没有接收落地
* 没有来源记录

因此：

* 无法可靠区分消息是本地产生还是远端转发
* 无法方便做环路抑制

---

## 15.2 设计4的优势

有了 `recv` 表之后，可以记录：

* 来源节点
* 来源 publication
* 远端消息身份
* 接收状态

这为后续实现：

* 级联转发策略
* 循环订阅抑制
* hop 计数
* origin 识别

提供了基础。

---

## 15.3 当前阶段限制

设计4虽然为级联/循环提供基础，但第一版可以只做到：

* 记录来源
* 不把收到的远端消息重新写入 `pg_publication_sync`

这就已经比设计3稳很多。

---

# 16. 清理与生命周期

---

## 16.1 生命周期不同于 `sync`

`pg_publication_sync` 是 append-only 发布日志表；
`pg_publication_recv` 是接收与状态表，因此生命周期不同。

---

## 16.2 清理策略

`recv` 表的清理不应只看接收时间，而应结合状态：

### 可清理候选

* `state='S'`
* 或长期失败、人工确认后废弃的 `F/D`

### 不应清理

* `R`
* `A`
* 近期 `F`

---

## 16.3 清理目标

设计4的清理目标是：

* 清理已成功完成且审计期已过的消息
* 清理已确认不再重试的失败消息

---

# 17. 与前三期的关系

---

## 17.1 不是前置条件

设计4不是设计1/2/3成立的前置条件。
前三期已经可以形成：

> **可运行的最小闭环**

---

## 17.2 设计4的定位

设计4是：

> **增强型、可产品化、可运维的控制面方案**

它解决的是“怎么把 DDL 做稳”，而不是“DDL 能不能跑”。

---

# 18. 设计总结

设计4的本质可以概括为：

> **用一张接收与状态表，把 DDL 自动同步从“收到即执行”的轻量模型，升级为“先可靠接收、再受控执行”的增强模型。**

其关键价值在于：

1. 区分接收成功与执行成功
2. 提供失败状态可见性
3. 支持后续重试
4. 支持审计与排障
5. 支持多来源接收
6. 提供级联/循环订阅基础
7. 通过 DDL barrier 恢复 DDL / DML 顺序一致性

可以用一句话总结：

> **设计3解决“DDL 能跑”，设计4解决“DDL 能稳”。**
