# 逻辑复制支持同步系统表概要设计（总览）

更新时间：2026-04-17

## 1. 背景

当前实现的目标不是“放开所有系统表复制”，而是围绕 `pg_publication_sync` 做一条可控、可验证、可扩展的能力链：

1. 新增并稳定 `pg_publication_sync` 系统目录模型。
2. 在逻辑复制链路上支持“白名单系统表同步”。
3. 基于 `pg_publication_sync` 完成 DDL 捕获、同步、订阅端 apply，实现自动 DDL 同步。

这三件事是解耦设计、分层实现、独立验证的。

## 2. 文档拆分

本次将设计与验证文档拆分为“3+1”：

1. 总体设计（本文）
2. 设计1：新增系统表  
   [新增逻辑复制同步系统表.md](/Users/growduduan/cwork/postgresql-2/新增逻辑复制同步系统表.md)
3. 设计2：逻辑复制支持同步系统表  
   [逻辑复制支持同步系统表-设计.md](/Users/growduduan/cwork/postgresql-2/逻辑复制支持同步系统表-设计.md)
4. 设计3：利用逻辑复制同步 `pg_publication_sync` 并 apply DDL  
   [pg_publication_sync自动DDL同步-设计.md](/Users/growduduan/cwork/postgresql-2/pg_publication_sync自动DDL同步-设计.md)

每个设计对应一份可独立执行的测试验证文档：

1. [新增系统表-测试验证步骤.md](/Users/growduduan/cwork/postgresql-2/新增系统表-测试验证步骤.md)
2. [逻辑复制支持同步系统表-测试验证步骤.md](/Users/growduduan/cwork/postgresql-2/逻辑复制支持同步系统表-测试验证步骤.md)
3. [pg_publication_sync自动DDL同步-测试验证步骤.md](/Users/growduduan/cwork/postgresql-2/pg_publication_sync自动DDL同步-测试验证步骤.md)

## 3. 分层能力边界

### 3.1 设计1：新增系统表（目录模型层）

只解决“元数据承载”问题：

* 系统表结构、字段语义、索引与依赖关系；
* 统一消息模型（仅 `Q/A/D`）的数据组织；
* 目录表采用“无行级 `oid` 字段优先”的模型；
* 表采用 append-only（只 INSERT，不 DELETE）；
* 不涉及 WAL、pgoutput、订阅端 apply 执行逻辑。

### 3.2 设计2：逻辑复制支持同步系统表（数据面层）

只解决“系统表行能通过逻辑复制到达订阅端”：

* 白名单机制（仅允许指定系统表）；
* WAL 记录、publication publishable 判断、pgoutput 发布、subscriber 目标关系检查；
* 不要求 DDL 自动执行。

### 3.3 设计3：`pg_publication_sync` 自动 DDL 同步（控制面执行层）

只解决“收到系统表行后如何解释并执行”：

* 发布端 DDL 捕获与过滤；
* `Q/A/D` 消息协议；
* `Q` 按 publication 粒度展开写入；
* 发布端按 `pfsyncpubid` 路由发送，订阅端不再依赖 `publication_list`；
* 订阅端 apply 分发、执行与订阅关系维护；
* 发布端只给出“可发布事实”，订阅端独立做“可订阅决策”。

## 4. 端到端数据流（组合后能力）

1. `ProcessUtility` 捕获 DDL，写入 `pg_publication_sync` 对象行。
2. 白名单系统表同步能力将这些行发送至订阅端。
3. 订阅端 apply worker 先写本地 `pg_publication_sync`，再按 `message_type` 执行：
   * `Q`：按发布端 `search_path` 重放 DDL SQL；
   * `A/D`：调整 `pg_subscription_rel` 映射。
4. 历史消息通过 `pg_publication_sync_prune()` 按 LSN+时间窗口清理。

## 5. 验收策略

验收按“单层先过，再做组合”：

1. 先跑设计1测试：确认系统表模型正确。
2. 再跑设计2测试：确认系统表能稳定复制。
3. 最后跑设计3测试：确认自动 DDL 同步行为正确。

通过后再执行复杂场景集成回归：
[逻辑复制DDL复杂场景集成测试用例.md](/Users/growduduan/cwork/postgresql-2/逻辑复制DDL复杂场景集成测试用例.md)
