# 测试1：新增系统表 `pg_publication_sync` 验证步骤

对应设计文档：  
[新增逻辑复制同步系统表.md](/Users/growduduan/cwork/postgresql-2/新增逻辑复制同步系统表.md)

## 1. 目标

独立验证目录模型：

1. 无行级 `oid`；
2. 仅消息行（`Q/A/D`）；
3. append-only（只写不删）。

## 2. 前置条件

1. 重新 `initdb`（catalog 已变更）；
2. 单节点 superuser 连接。

## 3. 步骤

### 3.1 结构检查

```sql
\d+ pg_catalog.pg_publication_sync
```

预期：

1. 不存在 `oid` 字段；
2. 存在 `pfsyncmsgtype/pfsynctargettable/pfsyncddlsql/pfsyncsearchpath`；
3. 存在 `pfsynclsn/pfsyncts`；
4. 存在 `pg_publication_sync_map_index` 与 `pg_publication_sync_pubid_index`。

### 3.2 `Q` 消息写入（按 publication fan-out）

```sql
CREATE TABLE t_sys_meta_base(id int primary key);
CREATE PUBLICATION pub_sys_meta_a FOR TABLE t_sys_meta_base WITH (ddl = 'table');
CREATE PUBLICATION pub_sys_meta_b FOR TABLE t_sys_meta_base WITH (ddl = 'table');

CREATE TABLE t_sys_meta_q(id int);

SELECT p.pubname, s.pfsyncmsgtype
  FROM pg_publication_sync s
  JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE s.pfsyncmsgtype = 'Q'
   AND position('CREATE TABLE t_sys_meta_q' in s.pfsyncddlsql) > 0
 ORDER BY p.pubname;
```

预期：

1. 返回两行；
2. `pub_sys_meta_a/pub_sys_meta_b` 各一行；
3. 证明一条 DDL 按 publication 粒度展开写入。

### 3.3 `A/D` 消息写入

```sql
CREATE TABLE t_sys_meta_ad(id int primary key);
ALTER PUBLICATION pub_sys_meta_a ADD TABLE t_sys_meta_ad;
ALTER PUBLICATION pub_sys_meta_a DROP TABLE t_sys_meta_ad;

SELECT s.pfsyncmsgtype, s.pfsynctargettable
  FROM pg_publication_sync s
  JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE p.pubname = 'pub_sys_meta_a'
   AND s.pfsynctargettable = 'public.t_sys_meta_ad'
 ORDER BY s.pfsyncobjid;
```

预期：包含 `A` 和 `D` 两类消息。

### 3.4 清理函数

```sql
SELECT pg_publication_sync_prune();
```

预期：函数可执行并返回删除行数（`int8`）。

## 4. 清理

```sql
DROP PUBLICATION IF EXISTS pub_sys_meta_a;
DROP PUBLICATION IF EXISTS pub_sys_meta_b;
DROP TABLE IF EXISTS t_sys_meta_ad;
DROP TABLE IF EXISTS t_sys_meta_q;
DROP TABLE IF EXISTS t_sys_meta_base;
```
