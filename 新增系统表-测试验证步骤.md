# 测试1：新增系统表 `pg_publication_sync` 验证步骤

对应设计文档：  
[新增逻辑复制同步系统表.md](/Users/growduduan/cwork/postgresql-2/新增逻辑复制同步系统表.md)

## 1. 目标

独立验证系统表模型本身：

1. 表结构与索引正确。
2. publication 默认行（`p`）可创建与更新。
3. publication 成员变更消息行（`A/D`）可写入。

本测试不依赖订阅端，不验证复制与 apply。

## 2. 前置条件

1. 使用当前分支重新 `initdb`（catalog 已变更）。
2. 单节点 superuser 连接数据库。

## 3. 验证步骤

### 3.1 校验系统表结构

```sql
\d+ pg_catalog.pg_publication_sync
```

预期：

1. 字段包含 `message_type/target_table/ddl_str/publication_list/search_path`。
2. 能看到 `pfsyncddl` 等核心固定列。
3. 索引包含主键索引与 map 唯一索引。

### 3.2 校验 publication 默认行（`p`）创建

```sql
CREATE TABLE t_sys_meta_base(id int primary key);
CREATE PUBLICATION pub_sys_meta
FOR TABLE t_sys_meta_base
WITH (ddl = 'table,index');

SELECT s.pfsynckind, s.pfsyncenabled, s.pfsyncddl
  FROM pg_publication_sync s
  JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE p.pubname = 'pub_sys_meta'
   AND s.pfsynckind = 'p';
```

预期：

1. 查询返回 1 行。
2. `pfsyncenabled = true`。
3. `pfsyncddl` 对应 `table,index` mask。

### 3.3 校验 publication 默认行（`p`）更新

```sql
ALTER PUBLICATION pub_sys_meta
SET (ddl = 'table,index,trigger');

SELECT s.pfsynckind, s.pfsyncenabled, s.pfsyncddl
  FROM pg_publication_sync s
  JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE p.pubname = 'pub_sys_meta'
   AND s.pfsynckind = 'p';
```

预期：

1. 仍为 1 行（upsert，不重复插入）。
2. `pfsyncddl` 更新为新 mask。

### 3.4 校验 `A/D` 对象消息行写入

```sql
CREATE TABLE t_sys_meta_ad(id int primary key);

ALTER PUBLICATION pub_sys_meta ADD TABLE t_sys_meta_ad;
ALTER PUBLICATION pub_sys_meta DROP TABLE t_sys_meta_ad;

SELECT message_type, target_table, publication_list
  FROM pg_publication_sync s
  JOIN pg_publication p ON p.oid = s.pfsyncpubid
 WHERE p.pubname = 'pub_sys_meta'
   AND s.pfsynckind = 'o'
   AND target_table = 'public.t_sys_meta_ad'
 ORDER BY s.oid;
```

预期：

1. 至少两行消息。
2. 包含 `message_type='A'` 与 `message_type='D'`。
3. `target_table='public.t_sys_meta_ad'`，`publication_list` 包含 `pub_sys_meta`。

## 4. 清理

```sql
DROP PUBLICATION IF EXISTS pub_sys_meta;
DROP TABLE IF EXISTS t_sys_meta_ad;
DROP TABLE IF EXISTS t_sys_meta_base;
```

